%ul#categorycrumb
  %li= link_to 'All categories', comfy_blog_posts_path(blog_path: @blog.path)
  - @blog.categories.order("LOWER(name) ASC").each do |category|
    %li= link_to category.name, comfy_blog_posts_path(category: category.name)
