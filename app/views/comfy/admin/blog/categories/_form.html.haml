= form.text_field :name
= form.form_group :class => 'form-actions' do
  = form.submit :class => 'btn btn-primary'
  = link_to t('.cancel'), comfy_admin_blog_categories_path(@site, @blog), :class => 'btn btn-link'
